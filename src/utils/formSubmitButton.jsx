import React from 'react';

function SubmitButton({ onClick, style }) {
    return (
        <button form='form1' type="submit" >
            Save
        </button>
    );
}

export default SubmitButton;
